# Torbert public mirror analysis

This mirror reflects the validated Torbert surface. Character charging, account-scoped free use, authenticated spend, and stable retry IDs are documented and fail closed on uncertain billing. Transform/undo actions are primary; advanced prompts are optional. Support logs omit note text and secrets. Private-source typecheck, tests, build, and diff-check precede publication.
